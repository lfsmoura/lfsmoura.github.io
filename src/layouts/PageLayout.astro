---
import BaseLayout from './BaseLayout.astro';

interface Props {
  title?: string;
}

const { title } = Astro.props;
---

<BaseLayout title={title}>
  <div class="w-full">
    {title && <h1>{title}</h1>}
    <slot />
  </div>
</BaseLayout>
